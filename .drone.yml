kind: pipeline
type: docker
name: secret-test

steps:
- name: check-secrets
  image: alpine

  environment:
    AWS_REGION:
      from_secret: AWS_REGION

    AWS_ACCOUNT_ID:
      from_secret: AWS_ACCOUNT_ID

  commands:
    - echo "REGION=$AWS_REGION"
    - echo "ACCOUNT=$AWS_ACCOUNT_ID"