kind: pipeline
type: docker
name: build

steps:
- name: build-image
  image: docker:24-cli

  volumes:
  - name: dockersock
    path: /var/run/docker.sock

  commands:
    - docker build -t drone-test:latest .

volumes:
- name: dockersock
  host:
    path: /var/run/docker.sock