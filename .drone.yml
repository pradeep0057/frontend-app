kind: pipeline
type: docker
name: build

steps:
- name: build-image
  image: docker:24-cli

  volumes:
  - name: dockersock
    path: /var/run/docker.sock

  commands:
    - docker build -t frontend-app:latest .
    - docker images

volumes:
- name: dockersock
  host:
    path: /var/run/docker.sock