kind: pipeline
type: docker
name: docker-test

steps:
- name: docker-info
  image: docker:24-cli

  volumes:
  - name: dockersock
    path: /var/run/docker.sock

  commands:
    - docker version
    - docker ps

volumes:
- name: dockersock
  host:
    path: /var/run/docker.sock